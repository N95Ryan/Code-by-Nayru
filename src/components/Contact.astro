---
interface ContactInfo {
  icon: string;
  label: string;
  value: string;
}

const contactInfo: ContactInfo[] = [
  {
    icon: "📧",
    label: "Email",
    value: "contact@codebynayru.com",
  },
  {
    icon: "📱",
    label: "Téléphone",
    value: "+33 6 12 34 56 78",
  },
  {
    icon: "📍",
    label: "Localisation",
    value: "Paris, France",
  },
];
---

<section id="contact" class="py-20 text-white">
  <div class="container mx-auto px-4">
    <h2 class="text-3xl md:text-4xl font-bold text-center mb-12">
      Contactez-moi
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
      <!-- Formulaire de contact -->
      <div class="bg-gray-900 p-8 rounded-lg">
        <form class="space-y-6">
          <div>
            <label for="name" class="block text-sm font-medium mb-2">Nom</label>
            <input
              type="text"
              id="name"
              name="name"
              class="w-full px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:border-purple-600"
              required
            />
          </div>

          <div>
            <label for="email" class="block text-sm font-medium mb-2"
              >Email</label
            >
            <input
              type="email"
              id="email"
              name="email"
              class="w-full px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:border-purple-600"
              required
            />
          </div>

          <div>
            <label for="message" class="block text-sm font-medium mb-2"
              >Message</label
            >
            <textarea
              id="message"
              name="message"
              rows="4"
              class="w-full px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:border-purple-600"
              required></textarea>
          </div>

          <button
            type="submit"
            class="w-full bg-purple-600 hover:bg-purple-700 text-white py-3 rounded-lg transition-colors"
          >
            Envoyer le message
          </button>
        </form>
      </div>

      <!-- Informations de contact -->
      <div class="space-y-8">
        <div class="bg-gray-900 p-8 rounded-lg">
          <h3 class="text-xl font-bold mb-6">Informations de contact</h3>
          <div class="space-y-4">
            {
              contactInfo.map((info) => (
                <div class="flex items-start">
                  <span class="text-2xl mr-4">{info.icon}</span>
                  <div>
                    <p class="font-medium">{info.label}</p>
                    <p class="text-gray-300">{info.value}</p>
                  </div>
                </div>
              ))
            }
          </div>
        </div>

        <div class="bg-gray-900 p-8 rounded-lg">
          <h3 class="text-xl font-bold mb-6">Horaires de disponibilité</h3>
          <p class="text-gray-300">Lundi - Vendredi : 9h - 18h</p>
          <p class="text-gray-300">Samedi : 10h - 14h</p>
          <p class="text-gray-300">Dimanche : Fermé</p>
        </div>
      </div>
    </div>
  </div>
</section>
